BEGIN;
CREATE TABLE IF NOT EXISTS "GamePatches" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    "Name" varchar(100) NOT NULL,
    "Description" varchar(2000) NOT NULL DEFAULT '',
    "SortOrder" integer NOT NULL DEFAULT 0,
    "IsArchived" boolean NOT NULL DEFAULT false,
    "IsDeleted" boolean NOT NULL DEFAULT false,
    "Version" integer NOT NULL DEFAULT 1
);
CREATE UNIQUE INDEX IF NOT EXISTS "IX_GamePatches_ActiveName" ON "GamePatches" (lower("Name")) WHERE NOT "IsDeleted";
CREATE TABLE IF NOT EXISTS "PatchRevisions" (
    "Id" uuid PRIMARY KEY,
    "PatchId" integer NOT NULL REFERENCES "GamePatches"("Id") ON DELETE RESTRICT,
    "Version" integer NOT NULL,
    "Action" varchar(30) NOT NULL,
    "Actor" varchar(256) NOT NULL,
    "Snapshot" text NOT NULL,
    "CreatedAtUtc" timestamptz NOT NULL
);
CREATE UNIQUE INDEX IF NOT EXISTS "IX_PatchRevisions_PatchId_Version" ON "PatchRevisions" ("PatchId", "Version");
ALTER TABLE "Spells" ADD COLUMN IF NOT EXISTS "PatchId" integer REFERENCES "GamePatches"("Id") ON DELETE RESTRICT;
CREATE INDEX IF NOT EXISTS "IX_Spells_PatchId" ON "Spells" ("PatchId");
ALTER TABLE "Items" ADD COLUMN IF NOT EXISTS "PatchId" integer REFERENCES "GamePatches"("Id") ON DELETE RESTRICT;
CREATE INDEX IF NOT EXISTS "IX_Items_PatchId" ON "Items" ("PatchId");
COMMIT;
